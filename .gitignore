Secure/
evidence/
*.kdbx
*.env

# Playwright browser automation: session profile (cookies/session state
# live here once logged in) and captured screenshots/reports -- never
# committed, regardless of how the browser/ directory structure evolves.
browser/profile/
browser/profile-opnsense/
browser/profile-kpn/
browser/artifacts/
browser/node_modules/

# Local triage agent's learned correlation stats (local-agent.mjs) --
# runtime state, not configuration; known-traffic.mjs is the actual
# human-reviewed source of truth.
browser/alert-dashboard/dismissal-stats.json
